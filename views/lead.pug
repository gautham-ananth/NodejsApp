doctype html
html(lang='en')
  include common/head.pug
  +head("Started MVP")
  body
    h1= Lead
    p  
        span #{lead.email}
        |
        a(href="/lead/" + lead.id + "/edit" ) Edit
        form(action="/lead/" + lead.id + "/delete", method="POST")
            button(type="subbmit") Delete
    include common/footer.pug 
    +footer()
    script(src="/javascripts/leads.js") 
