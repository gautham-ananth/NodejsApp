extends ../layout

block content
    form(action="/lead/" + lead.id + "/edit", method="POST")
        input(type="email", placeholder="Enter your email address", name="leadEmail", value = lead.email)
        button(type="submit") Save
